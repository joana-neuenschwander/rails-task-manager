<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %></li>
    <%# o (task) me mostra a instance de onde vai pegar o id e aí vai chegar até o path %>
  <% end %>
</ul>
<%= link_to "Create a new task!", tasks_new_path %></li>
